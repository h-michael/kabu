# yaml-language-server: $schema=https://raw.githubusercontent.com/h-michael/kabu/main/schema/kabu.schema.json

# When to use: Start from the smallest valid config.
# Run: kabu add ../wt-min
# Expected effect: Creates a worktree/workspace without extra setup operations.
# Pitfall: Do not use `defaults.on_conflict`; use top-level `on_conflict`.

# Optional global conflict mode
# on_conflict: backup
